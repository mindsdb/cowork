# Supply-chain hardening: do not resolve package versions published less
# than 7 days ago.  This reduces exposure to fast-moving npm supply-chain
# attacks (e.g. shai-hulud) by ensuring only versions that have survived
# community scrutiny are installed.
#
# Requires npm >= 11.10.0.  Silently ignored on older versions.
min-release-age=7
