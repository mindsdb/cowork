# Dev build — loaded by `vite build --mode dev`.
# Repo-controlled: edit values here, NOT via GitHub Actions repo vars.

VITE_KEYCLOAK_URL=https://auth.dev.mindshub.ai/auth
VITE_MINDS_API_URL=https://api.dev.mindshub.ai/v1
