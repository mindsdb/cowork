// Redesigned artifact workspace — M0 shared chassis (presentational shell).
// Import './redesign.css' once near your entrypoint; it defines the global
// keyframes these components reference.
export { TopBar } from './TopBar';
export { WorkspaceShell } from './WorkspaceShell';
