// scrubberIndex.js — barrel for the M2 "time travel you can see" surface.
//
// VersionScrubber → WorkspaceShell bottomStrip (the horizontal timeline under the canvas).
// HistoryPanel    → the dock "History" tab (vertical version-card feed).
export { VersionScrubber, MOCK_VERSIONS } from './VersionScrubber';
export { HistoryPanel } from './HistoryPanel';
