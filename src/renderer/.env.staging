# Staging build — loaded by `vite build --mode staging`.
# Repo-controlled: edit values here, NOT via GitHub Actions repo vars.

VITE_KEYCLOAK_URL=https://auth.staging.mindshub.ai/auth
VITE_MINDS_API_URL=https://api.staging.mindshub.ai/v1
