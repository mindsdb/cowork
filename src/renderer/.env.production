# Production build — loaded by `vite build --mode production`.
# Repo-controlled: edit values here, NOT via GitHub Actions repo vars.

VITE_KEYCLOAK_URL=https://auth.mindshub.ai/auth
VITE_MINDS_API_URL=https://api.mindshub.ai/v1
