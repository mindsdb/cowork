node_modules/
dist/
release/
coverage/
test-results/
playwright-report/
.vite/
*.js.map
.DS_Store
Thumbs.db
__pycache__/
*.pyc
*.pyo
.vscode
.env
.env.local

# Apple Developer
CertificateSigningRequest.certSigningRequest
developerID_application.cer
--apple-id
# Build-time generated channel config (written by scripts/gen-build-channel.mjs)
src/main/build-channel.gen.ts
src/main/app-version.gen.txt

# ENG-1241: real OAuth secrets, written here transiently by CI
# (build-macos-pkg.yml / build-windows-installer.yml) alongside the checked-in
# postinstall script. Never meant to be committed.
build/pkg-scripts/server-credentials.json
# Same secrets, at the repo root — where the Windows and Linux build jobs write
# them. This repo is public; anyone reproducing those builds locally is
# otherwise one `git add -A` away from publishing them.
/server-credentials.json

src/main/updater-version.gen.txt
build/app-update.yml
